\frametitle{jcode 启动全链路（下）}
\begin{block}{修复在 QEMU 运行过程中的位置（续）}
    \begin{lstlisting}[basicstyle=\ttfamily\tiny, frame=none]
    |
    v
[serve 发出 HTTPS 请求]
  非阻塞 connect Waker 正确注册          (Fix 4)
  ARP 队列足够大                         (Fix 6a)
  TCP 握手完成后 epoll 正确通知          (Fix 4)
    |
    v
[AI 响应流回来]
  ARP 缓存 300s 不过期                   (Fix 6b)
  ARP 队列扫描发送                       (Fix 6c)
  epoll EPOLLET 连续消息正常通知         (Fix 3b)
    |
    v
[会话保存]
  rename 后 write_at 用 inode 号         (Fix 1b)
    |
    v
[用户鼠标操作]
  ostool 转发点击/滚动                   (Fix 8a)
  TIOCSWINSZ 发送 SIGWINCH               (Fix 7b)
    |
    v
[TUI 输出到终端]
  批量 UART 写，整帧原子发送             (Fix 7c)
  COW 缺页安全处理                       (Fix 2)\end{lstlisting}
  \end{block}
